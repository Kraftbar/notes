# gtk-lang

GTK3 + Glade learning exercise in C, from the former `gtk-lang` repo (2020,
23 commits). A language-practice window built with GtkBuilder loading
`glade/window_main.glade`, plus a small HTML/JS variant under `_html/`.

Build with `make`. Run from this directory so the Glade path resolves.
